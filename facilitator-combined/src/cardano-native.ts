import { createHash, createPublicKey, verify as verifySignature } from "node:crypto";
import { Hono } from "hono";
import { z } from "zod";
import { blake2b } from "./blake2b.js";
import { bearerSecretMatches } from "./secret.js";

const CARDANO_TRANSACTION_MAX_BYTES = 64 * 1024;
const CARDANO_MAX_INPUTS = 64;
const CARDANO_UTXO_PAGE_LIMIT = 10;
const CARDANO_UTXO_PAGE_SIZE = 100;
const CARDANO_TTL_SLOP_SLOTS = 30n;
const ED25519_SPKI_PREFIX = Buffer.from("302a300506032b6570032100", "hex");
const BECH32_ALPHABET = "qpzry9x8gf2tvdw0s3jn54khce6mua7l";
const CARDANO_ASSET_UNIT = /^[0-9a-f]{56}(?:[0-9a-f]{2}){0,32}$/;
const RESOURCE_BINDING = /^[0-9a-f]{64}$/;

type CardanoNetwork = "cardano:preprod" | "cardano:mainnet";
type Requirement = {
  scheme: "exact";
  network: CardanoNetwork;
  amount: string;
  payTo: string;
  asset: string;
  maxTimeoutSeconds: number;
  extra: Record<string, unknown>;
};
type ExactPayload = {
  x402Version: 2;
  accepted: Requirement;
  payload: { transaction: string; nonce: string; submissionMode?: "server" };
};
type CborNode = { value: unknown; start: number; end: number; children?: CborNode[]; entries?: Array<[CborNode,CborNode]> };
type CardanoValue = { lovelace: bigint; token: bigint };

const requirementSchema = z.object({
  scheme: z.literal("exact"),
  network: z.enum(["cardano:preprod","cardano:mainnet"]),
  amount: z.string().regex(/^[1-9]\d*$/),
  payTo: z.string().min(20).max(200),
  asset: z.string().min(1).max(120),
  maxTimeoutSeconds: z.number().int().min(2).max(3600),
  extra: z.record(z.string(),z.unknown()).default({}),
});
const payloadSchema = z.object({
  x402Version:z.literal(2),
  accepted:requirementSchema,
  payload:z.object({transaction:z.string().min(4),nonce:z.string().regex(/^[0-9a-f]{64}#\d+$/),submissionMode:z.literal("server").optional()}),
});
const requestSchema=z.object({paymentPayload:payloadSchema,paymentRequirements:requirementSchema});
const signerResponseSchema=z.object({transaction:z.string().min(4),nonce:z.string().regex(/^[0-9a-f]{64}#\d+$/),transactionId:z.string().regex(/^[0-9a-f]{64}$/).optional()});

export const cardanoNativeEnvSchema=z.object({
  APP_ENV:z.enum(["development","test","production"]).default("development"),
  CARDANO_NETWORK:z.enum(["preprod","mainnet"]).default("preprod"),
  CARDANO_PAYER_ADDRESS:z.string().min(20).max(200),
  CARDANO_BLOCKFROST_URL:z.string().url(),
  CARDANO_BLOCKFROST_PROJECT_ID:z.string().min(20),
  CARDANO_SIGNER_URL:z.string().url(),
  CARDANO_SIGNER_API_KEY:z.string().min(32),
  CARDANO_SETTLEMENT_STORE_URL:z.string().url(),
  CARDANO_SETTLEMENT_STORE_API_KEY:z.string().min(32),
  CARDANO_USDCX_ASSET_ID:z.string().regex(CARDANO_ASSET_UNIT).optional(),
  MANAGED_SIGNING_API_KEY:z.string().min(32).optional(),
  SETTLEMENT_API_KEY:z.string().min(32).optional(),
  FACILITATOR_API_KEY:z.string().min(32).optional(),
  CARDANO_MAX_FEE_LOVELACE:z.string().regex(/^[1-9]\d*$/).default("1000000"),
  CARDANO_CONFIRMATION_TIMEOUT_MS:z.coerce.number().int().min(1000).max(300000).default(90000),
  CARDANO_CONFIRMATION_POLL_MS:z.coerce.number().int().min(250).max(30000).default(3000),
});
export type CardanoNativeEnv=z.infer<typeof cardanoNativeEnvSchema>;

function httpsOnly(name:string,value:string){if(new URL(value).protocol!=="https:")throw new Error(`${name} must use HTTPS in production`);}
function stable(value:unknown):string{if(value===null||typeof value!=="object")return JSON.stringify(value);if(Array.isArray(value))return`[${value.map(stable).join(",")}]`;return`{${Object.entries(value as Record<string,unknown>).sort(([a],[b])=>a.localeCompare(b)).map(([k,v])=>`${JSON.stringify(k)}:${stable(v)}`).join(",")}}`;}
function assertAssetUnit(value:string){const normalized=value.toLowerCase();if(!CARDANO_ASSET_UNIT.test(normalized))throw new Error("CARDANO_ASSET_UNIT_INVALID");return normalized;}
function allowedAsset(env:CardanoNativeEnv,asset:string){if(asset==="lovelace")return"lovelace";const normalized=assertAssetUnit(asset);if(!env.CARDANO_USDCX_ASSET_ID||normalized!==env.CARDANO_USDCX_ASSET_ID)throw new Error("CARDANO_ASSET_NOT_WHITELISTED");return normalized;}
function resourceBinding(requirement:Requirement){const value=requirement.extra.resourceBinding;if(typeof value!=="string"||!RESOURCE_BINDING.test(value))throw new Error("CARDANO_RESOURCE_BINDING_REQUIRED");return value;}
function sameRequirement(a:Requirement,b:Requirement){return a.scheme===b.scheme&&a.network===b.network&&a.amount===b.amount&&a.payTo===b.payTo&&a.asset===b.asset&&a.maxTimeoutSeconds===b.maxTimeoutSeconds&&stable(a.extra)===stable(b.extra);}

export function parseCardanoNativeEnv(input:unknown=process.env):CardanoNativeEnv{
  const env=cardanoNativeEnvSchema.parse(input);const network=`cardano:${env.CARDANO_NETWORK}` as CardanoNetwork;assertAddressNetwork(env.CARDANO_PAYER_ADDRESS,network);
  if(env.APP_ENV!=="production"&&env.CARDANO_NETWORK==="mainnet")throw new Error("Cardano Mainnet is prohibited outside production");
  if(env.APP_ENV==="production"){
    if(!env.MANAGED_SIGNING_API_KEY||!env.SETTLEMENT_API_KEY)throw new Error("Production Cardano facilitator capability keys are required");
    httpsOnly("CARDANO_BLOCKFROST_URL",env.CARDANO_BLOCKFROST_URL);httpsOnly("CARDANO_SIGNER_URL",env.CARDANO_SIGNER_URL);httpsOnly("CARDANO_SETTLEMENT_STORE_URL",env.CARDANO_SETTLEMENT_STORE_URL);
    const secrets=[env.MANAGED_SIGNING_API_KEY,env.SETTLEMENT_API_KEY,env.CARDANO_SIGNER_API_KEY,env.CARDANO_SETTLEMENT_STORE_API_KEY];if(new Set(secrets).size!==secrets.length)throw new Error("Production Cardano capability, signer, and settlement-store secrets must be distinct");
  }
  return env;
}

async function boundedJson(request:Request,maxBytes=128*1024){const declared=Number(request.headers.get("content-length")??"0");if(Number.isFinite(declared)&&declared>maxBytes)throw new Error("REQUEST_BODY_TOO_LARGE");const text=await request.text();if(Buffer.byteLength(text,"utf8")>maxBytes)throw new Error("REQUEST_BODY_TOO_LARGE");return JSON.parse(text) as unknown;}

function cborLength(buffer:Buffer,offset:number,additional:number):{value:bigint|null;offset:number}{if(additional<24)return{value:BigInt(additional),offset};if(additional===24)return{value:BigInt(buffer.readUInt8(offset)),offset:offset+1};if(additional===25)return{value:BigInt(buffer.readUInt16BE(offset)),offset:offset+2};if(additional===26)return{value:BigInt(buffer.readUInt32BE(offset)),offset:offset+4};if(additional===27)return{value:buffer.readBigUInt64BE(offset),offset:offset+8};if(additional===31)return{value:null,offset};throw new Error("CBOR_LENGTH_UNSUPPORTED");}
function parseCbor(buffer:Buffer,start=0):CborNode{if(start>=buffer.length)throw new Error("CBOR_TRUNCATED");const initial=buffer[start],major=initial>>5,additional=initial&31;let offset=start+1;const length=cborLength(buffer,offset,additional);offset=length.offset;
  if(major===0){if(length.value===null)throw new Error("CBOR_UINT_INDEFINITE");return{value:length.value,start,end:offset};}
  if(major===1){if(length.value===null)throw new Error("CBOR_NEGATIVE_INDEFINITE");return{value:-1n-length.value,start,end:offset};}
  if(major===2||major===3){if(length.value===null)throw new Error("CBOR_INDEFINITE_STRING_UNSUPPORTED");const size=Number(length.value);if(!Number.isSafeInteger(size)||offset+size>buffer.length)throw new Error("CBOR_TRUNCATED");const bytes=buffer.subarray(offset,offset+size);return{value:major===2?Buffer.from(bytes):bytes.toString("utf8"),start,end:offset+size};}
  if(major===4){const children:CborNode[]=[];if(length.value===null){while(true){if(offset>=buffer.length)throw new Error("CBOR_TRUNCATED");if(buffer[offset]===0xff){offset++;break;}const child=parseCbor(buffer,offset);children.push(child);offset=child.end;}}else{const count=Number(length.value);if(!Number.isSafeInteger(count)||count>10000)throw new Error("CBOR_ARRAY_TOO_LARGE");for(let i=0;i<count;i++){const child=parseCbor(buffer,offset);children.push(child);offset=child.end;}}return{value:children.map(c=>c.value),children,start,end:offset};}
  if(major===5){const entries:Array<[CborNode,CborNode]>=[];if(length.value===null){while(true){if(offset>=buffer.length)throw new Error("CBOR_TRUNCATED");if(buffer[offset]===0xff){offset++;break;}const key=parseCbor(buffer,offset);offset=key.end;const value=parseCbor(buffer,offset);offset=value.end;entries.push([key,value]);}}else{const count=Number(length.value);if(!Number.isSafeInteger(count)||count>10000)throw new Error("CBOR_MAP_TOO_LARGE");for(let i=0;i<count;i++){const key=parseCbor(buffer,offset);offset=key.end;const value=parseCbor(buffer,offset);offset=value.end;entries.push([key,value]);}}return{value:new Map(entries.map(([k,v])=>[k.value,v.value])),entries,start,end:offset};}
  if(major===6){if(length.value===null)throw new Error("CBOR_TAG_INDEFINITE");const child=parseCbor(buffer,offset);return{value:{tag:length.value,value:child.value},children:[child],start,end:child.end};}
  if(major===7){if(additional===20)return{value:false,start,end:offset};if(additional===21)return{value:true,start,end:offset};if(additional===22||additional===23)return{value:null,start,end:offset};throw new Error("CBOR_SIMPLE_UNSUPPORTED");}
  throw new Error("CBOR_MAJOR_UNSUPPORTED");}
function untag(node:CborNode):CborNode{let current=node;while(current.children?.length===1&&current.value&&typeof current.value==="object"&&"tag" in(current.value as object))current=current.children[0];return current;}
function asArray(node:CborNode,code:string){const v=untag(node);if(!v.children||!Array.isArray(v.value))throw new Error(code);return v.children;}
function asMap(node:CborNode,code:string){const v=untag(node);if(!v.entries||!(v.value instanceof Map))throw new Error(code);return v;}
function mapEntry(node:CborNode,key:bigint){return asMap(node,"CBOR_MAP_REQUIRED").entries!.find(([candidate])=>candidate.value===key)?.[1];}
function asBigInt(node:CborNode|undefined,code:string){if(!node||typeof node.value!=="bigint")throw new Error(code);return node.value;}
function asBytes(node:CborNode|undefined,code:string){if(!node||!Buffer.isBuffer(node.value))throw new Error(code);return node.value as Buffer;}

function bech32Polymod(values:number[]){const g=[0x3b6a57b2,0x26508e6d,0x1ea119fa,0x3d4233dd,0x2a1462b3];let chk=1;for(const value of values){const top=chk>>>25;chk=((chk&0x1ffffff)<<5)^value;for(let i=0;i<5;i++)if((top>>>i)&1)chk^=g[i];}return chk>>>0;}
function bech32ExpandHrp(hrp:string){return[...hrp].map(c=>c.charCodeAt(0)>>5).concat(0,[...hrp].map(c=>c.charCodeAt(0)&31));}
function convertBits(values:number[],fromBits:number,toBits:number){let acc=0,bits=0;const result:number[]=[],maxv=(1<<toBits)-1;for(const value of values){if(value<0||value>>fromBits!==0)throw new Error("BECH32_DATA_INVALID");acc=(acc<<fromBits)|value;bits+=fromBits;while(bits>=toBits){bits-=toBits;result.push((acc>>bits)&maxv);}}if(bits>=fromBits||((acc<<(toBits-bits))&maxv)!==0)throw new Error("BECH32_PADDING_INVALID");return Buffer.from(result);}
function decodeAddress(address:string){if(address!==address.toLowerCase())throw new Error("CARDANO_ADDRESS_CASE_INVALID");const sep=address.lastIndexOf("1");if(sep<1||sep+7>address.length)throw new Error("CARDANO_ADDRESS_INVALID");const hrp=address.slice(0,sep),values=[...address.slice(sep+1)].map(c=>BECH32_ALPHABET.indexOf(c));if(values.some(v=>v<0)||bech32Polymod([...bech32ExpandHrp(hrp),...values])!==1)throw new Error("CARDANO_ADDRESS_CHECKSUM_INVALID");const bytes=convertBits(values.slice(0,-6),5,8);if(bytes.length<29)throw new Error("CARDANO_ADDRESS_LENGTH_INVALID");return{hrp,bytes,networkId:bytes[0]&15,type:bytes[0]>>4};}
function assertAddressNetwork(address:string,network:CardanoNetwork){const decoded=decodeAddress(address),expected=network==="cardano:mainnet"?1:0,hrp=network==="cardano:mainnet"?"addr":"addr_test";if(decoded.networkId!==expected||decoded.hrp!==hrp)throw new Error("CARDANO_ADDRESS_NETWORK_MISMATCH");return decoded;}
function paymentKeyHash(address:string,network:CardanoNetwork){const d=assertAddressNetwork(address,network);if(![0,2,4,6].includes(d.type))throw new Error("CARDANO_PAYER_KEY_CREDENTIAL_REQUIRED");return d.bytes.subarray(1,29);}

function decodeMultiAsset(node:CborNode,allowedUnit:string):bigint{const pair=asArray(node,"CARDANO_MULTI_ASSET_VALUE_INVALID");if(pair.length!==2)throw new Error("CARDANO_MULTI_ASSET_VALUE_INVALID");const policyMap=asMap(pair[1],"CARDANO_MULTI_ASSET_MAP_INVALID");if(policyMap.entries!.length!==1)throw new Error("CARDANO_UNRELATED_NATIVE_ASSET");const[policyNode,assetMapNode]=policyMap.entries![0];const policy=asBytes(policyNode,"CARDANO_POLICY_ID_INVALID");if(policy.length!==28)throw new Error("CARDANO_POLICY_ID_INVALID");const assetMap=asMap(assetMapNode,"CARDANO_ASSET_MAP_INVALID");if(assetMap.entries!.length!==1)throw new Error("CARDANO_UNRELATED_NATIVE_ASSET");const[nameNode,quantityNode]=assetMap.entries![0];const name=asBytes(nameNode,"CARDANO_ASSET_NAME_INVALID");if(name.length>32)throw new Error("CARDANO_ASSET_NAME_INVALID");const unit=`${policy.toString("hex")}${name.toString("hex")}`;if(unit!==allowedUnit)throw new Error("CARDANO_ASSET_NOT_WHITELISTED");const quantity=asBigInt(quantityNode,"CARDANO_ASSET_QUANTITY_INVALID");if(quantity<0n)throw new Error("CARDANO_ASSET_QUANTITY_INVALID");return quantity;}
function decodeValue(node:CborNode,asset:string):CardanoValue{const value=untag(node);if(typeof value.value==="bigint"){if(value.value<0n)throw new Error("CARDANO_OUTPUT_VALUE_INVALID");return{lovelace:value.value,token:0n};}if(asset==="lovelace")throw new Error("CARDANO_MULTI_ASSET_OUTPUT_UNSUPPORTED");const pair=asArray(value,"CARDANO_OUTPUT_VALUE_INVALID");if(pair.length!==2)throw new Error("CARDANO_OUTPUT_VALUE_INVALID");const lovelace=asBigInt(pair[0],"CARDANO_OUTPUT_VALUE_INVALID");if(lovelace<0n)throw new Error("CARDANO_OUTPUT_VALUE_INVALID");return{lovelace,token:decodeMultiAsset(value,asset)};}

function decodeSignedTransaction(transactionBase64:string,asset:string){if(transactionBase64.length>Math.ceil(CARDANO_TRANSACTION_MAX_BYTES/3)*4)throw new Error("CARDANO_TRANSACTION_TOO_LARGE");const bytes=Buffer.from(transactionBase64,"base64");if(!bytes.length||bytes.length>CARDANO_TRANSACTION_MAX_BYTES||bytes.toString("base64")!==transactionBase64)throw new Error("CARDANO_TRANSACTION_BASE64_INVALID");const root=parseCbor(bytes);if(root.end!==bytes.length)throw new Error("CARDANO_TRANSACTION_TRAILING_DATA");const txParts=asArray(root,"CARDANO_TRANSACTION_ARRAY_REQUIRED");if(txParts.length<2||txParts.length>4)throw new Error("CARDANO_TRANSACTION_SHAPE_INVALID");const body=asMap(txParts[0],"CARDANO_TRANSACTION_BODY_INVALID"),witnessSet=asMap(txParts[1],"CARDANO_WITNESS_SET_INVALID");if(txParts[2]&&txParts[2].value===false)throw new Error("CARDANO_TRANSACTION_PHASE2_INVALID");if(txParts[3]&&txParts[3].value!==null)throw new Error("CARDANO_AUXILIARY_DATA_UNSUPPORTED");const allowedBodyKeys=new Set([0n,1n,2n,3n,8n,15n]);for(const[key]of body.entries!)if(typeof key.value!=="bigint"||!allowedBodyKeys.has(key.value))throw new Error("CARDANO_PHASE1_OPERATION_UNSUPPORTED");for(const[key]of witnessSet.entries!)if(key.value!==0n)throw new Error("CARDANO_SCRIPT_OR_BOOTSTRAP_WITNESS_UNSUPPORTED");
  const inputNode=mapEntry(body,0n),outputNode=mapEntry(body,1n);if(!inputNode||!outputNode)throw new Error("CARDANO_TRANSACTION_IO_REQUIRED");const inputNodes=asArray(inputNode,"CARDANO_INPUTS_INVALID");if(!inputNodes.length||inputNodes.length>CARDANO_MAX_INPUTS)throw new Error("CARDANO_INPUT_COUNT_INVALID");const inputs=inputNodes.map(node=>{const pair=asArray(node,"CARDANO_INPUT_INVALID");if(pair.length!==2)throw new Error("CARDANO_INPUT_INVALID");const hash=asBytes(pair[0],"CARDANO_INPUT_HASH_INVALID"),index=asBigInt(pair[1],"CARDANO_INPUT_INDEX_INVALID");if(hash.length!==32||index<0n||index>65535n)throw new Error("CARDANO_INPUT_INVALID");return`${hash.toString("hex")}#${index}`;});
  const outputs=asArray(outputNode,"CARDANO_OUTPUTS_INVALID").map(node=>{const u=untag(node);let addressNode:CborNode|undefined,valueNode:CborNode|undefined;if(u.entries){const allowed=new Set([0n,1n]);for(const[key]of u.entries)if(typeof key.value!=="bigint"||!allowed.has(key.value))throw new Error("CARDANO_OUTPUT_FEATURE_UNSUPPORTED");addressNode=mapEntry(u,0n);valueNode=mapEntry(u,1n);}else{const items=asArray(u,"CARDANO_OUTPUT_INVALID");if(items.length!==2)throw new Error("CARDANO_OUTPUT_FEATURE_UNSUPPORTED");[addressNode,valueNode]=items;}return{addressBytes:asBytes(addressNode,"CARDANO_OUTPUT_ADDRESS_INVALID"),...decodeValue(valueNode!,asset)};});if(!outputs.length)throw new Error("CARDANO_OUTPUTS_INVALID");
  const fee=asBigInt(mapEntry(body,2n),"CARDANO_FEE_REQUIRED"),ttl=asBigInt(mapEntry(body,3n),"CARDANO_TTL_REQUIRED"),validityNode=mapEntry(body,8n),networkNode=mapEntry(body,15n),validityStart=validityNode?asBigInt(validityNode,"CARDANO_VALIDITY_START_INVALID"):undefined,networkId=networkNode?asBigInt(networkNode,"CARDANO_NETWORK_ID_INVALID"):undefined;if(fee<0n||ttl<0n)throw new Error("CARDANO_TRANSACTION_NUMERIC_INVALID");
  const bodyHash=blake2b(bytes.subarray(body.start,body.end),32),transactionId=bodyHash.toString("hex"),vkeyNode=mapEntry(witnessSet,0n),witnesses=vkeyNode?asArray(vkeyNode,"CARDANO_VKEY_WITNESSES_INVALID"):[];if(!witnesses.length)throw new Error("CARDANO_TRANSACTION_UNSIGNED");const witnessKeys:Buffer[]=[];for(const witness of witnesses){const pair=asArray(witness,"CARDANO_VKEY_WITNESS_INVALID");if(pair.length!==2)throw new Error("CARDANO_VKEY_WITNESS_INVALID");const vkey=asBytes(pair[0],"CARDANO_VKEY_INVALID"),signature=asBytes(pair[1],"CARDANO_SIGNATURE_INVALID");if(vkey.length!==32||signature.length!==64)throw new Error("CARDANO_VKEY_WITNESS_INVALID");const publicKey=createPublicKey({key:Buffer.concat([ED25519_SPKI_PREFIX,vkey]),format:"der",type:"spki"});if(!verifySignature(null,bodyHash,publicKey,signature))throw new Error("CARDANO_INVALID_SIGNATURE");witnessKeys.push(vkey);}return{bytes,transactionId,inputs,outputs,fee,ttl,validityStart,networkId,witnessKeys};}

async function jsonFetch(env:CardanoNativeEnv,path:string,init?:RequestInit,allowNotFound=false){const response=await fetch(`${env.CARDANO_BLOCKFROST_URL.replace(/\/$/,"")}${path}`,{...init,redirect:"error",cache:"no-store",headers:{project_id:env.CARDANO_BLOCKFROST_PROJECT_ID,accept:"application/json",...(init?.headers??{})},signal:init?.signal??AbortSignal.timeout(10000)});if(allowNotFound&&response.status===404)return null;if(!response.ok)throw new Error(`CARDANO_PROVIDER_${response.status}`);return response.json();}
const txUtxosSchema=z.object({hash:z.string().regex(/^[0-9a-f]{64}$/),outputs:z.array(z.object({output_index:z.number().int().nonnegative(),address:z.string(),amount:z.array(z.object({unit:z.string(),quantity:z.string().regex(/^\d+$/)}))}))});
const addressUtxoSchema=z.array(z.object({tx_hash:z.string().regex(/^[0-9a-f]{64}$/),output_index:z.number().int().nonnegative()}));
const latestBlockSchema=z.object({height:z.number().int().nonnegative(),slot:z.number().int().nonnegative().nullable()});
const transactionEvidenceSchema=z.object({hash:z.string().regex(/^[0-9a-f]{64}$/),block_height:z.number().int().nonnegative(),valid_contract:z.boolean().optional()});
function blockfrostValue(rows:Array<{unit:string;quantity:string}>,asset:string):CardanoValue{let lovelace=0n,token=0n,seenLovelace=false,seenToken=false;for(const row of rows){if(row.unit==="lovelace"){if(seenLovelace)throw new Error("CARDANO_INPUT_VALUE_DUPLICATE");seenLovelace=true;lovelace=BigInt(row.quantity);}else if(asset!=="lovelace"&&row.unit.toLowerCase()===asset){if(seenToken)throw new Error("CARDANO_INPUT_VALUE_DUPLICATE");seenToken=true;token=BigInt(row.quantity);}else throw new Error("CARDANO_UNRELATED_NATIVE_ASSET");}if(!seenLovelace)throw new Error("CARDANO_INPUT_LOVELACE_MISSING");return{lovelace,token};}
async function sourceOutput(env:CardanoNativeEnv,ref:string,asset:string){const[hash,indexRaw]=ref.split("#"),index=Number(indexRaw),data=txUtxosSchema.parse(await jsonFetch(env,`/txs/${hash}/utxos`));if(data.hash!==hash)throw new Error("CARDANO_INPUT_EVIDENCE_HASH_MISMATCH");const output=data.outputs.find(row=>row.output_index===index);if(!output)throw new Error("CARDANO_INPUT_EVIDENCE_MISSING");return{...output,value:blockfrostValue(output.amount,asset)};}
async function nonceIsUnspent(env:CardanoNativeEnv,payer:string,nonce:string){const[hash,indexRaw]=nonce.split("#"),index=Number(indexRaw);for(let page=1;page<=CARDANO_UTXO_PAGE_LIMIT;page++){const rows=addressUtxoSchema.parse(await jsonFetch(env,`/addresses/${encodeURIComponent(payer)}/utxos?count=${CARDANO_UTXO_PAGE_SIZE}&page=${page}&order=asc`));if(rows.some(row=>row.tx_hash===hash&&row.output_index===index))return true;if(rows.length<CARDANO_UTXO_PAGE_SIZE)return false;}throw new Error("CARDANO_PAYER_UTXO_SET_TOO_LARGE");}

function claimBinding(requirement:Requirement,nonce:string,payer:string){return createHash("sha256").update(stable({requirement,nonce,payer})).digest("hex");}
const claimSchema=z.object({data:z.object({transactionHash:z.string(),network:z.string(),bindingHash:z.string(),state:z.enum(["CLAIMED","SUBMISSION_STARTED","CONFIRMED","REJECTED"])})});
async function claimRequest(env:CardanoNativeEnv,action:"CLAIM"|"MARK_SUBMISSION_STARTED"|"CONFIRM"|"REJECT",transactionHash:string,bindingHash:string){const response=await fetch(env.CARDANO_SETTLEMENT_STORE_URL,{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${env.CARDANO_SETTLEMENT_STORE_API_KEY}`},body:JSON.stringify({action,transactionHash,network:`cardano:${env.CARDANO_NETWORK}`,bindingHash}),signal:AbortSignal.timeout(10000)});if(response.status===409)throw new Error("CARDANO_SETTLEMENT_REPLAY");if(!response.ok)throw new Error(`CARDANO_SETTLEMENT_STORE_${response.status}`);return claimSchema.parse(await response.json()).data;}
async function readClaim(env:CardanoNativeEnv,transactionHash:string){const url=new URL(env.CARDANO_SETTLEMENT_STORE_URL);url.searchParams.set("transactionHash",transactionHash);const response=await fetch(url,{headers:{authorization:`Bearer ${env.CARDANO_SETTLEMENT_STORE_API_KEY}`},signal:AbortSignal.timeout(10000)});if(response.status===404)return null;if(!response.ok)throw new Error(`CARDANO_SETTLEMENT_STORE_${response.status}`);return claimSchema.parse(await response.json()).data;}

async function verifyTransaction(env:CardanoNativeEnv,payload:ExactPayload,requirement:Requirement){const network=`cardano:${env.CARDANO_NETWORK}` as CardanoNetwork;if(requirement.network!==network||payload.accepted.network!==network)throw new Error("CARDANO_NETWORK_MISMATCH");if(!sameRequirement(payload.accepted,requirement))throw new Error("CARDANO_REQUIREMENT_MISMATCH");if((payload.payload.submissionMode??"server")!=="server")throw new Error("CARDANO_SUBMISSION_MODE_MISMATCH");const asset=allowedAsset(env,requirement.asset);resourceBinding(requirement);const payerDecoded=assertAddressNetwork(env.CARDANO_PAYER_ADDRESS,network),payeeDecoded=assertAddressNetwork(requirement.payTo,network),transaction=decodeSignedTransaction(payload.payload.transaction,asset);if(!transaction.inputs.includes(payload.payload.nonce))throw new Error("CARDANO_NONCE_NOT_IN_INPUTS");const expectedNetworkId=network==="cardano:mainnet"?1n:0n;if(transaction.networkId!==undefined&&transaction.networkId!==expectedNetworkId)throw new Error("CARDANO_NETWORK_ID_MISMATCH");if(transaction.fee>BigInt(env.CARDANO_MAX_FEE_LOVELACE))throw new Error("CARDANO_FEE_LIMIT_EXCEEDED");const payerKey=paymentKeyHash(env.CARDANO_PAYER_ADDRESS,network);if(!transaction.witnessKeys.some(vkey=>blake2b(vkey,28).equals(payerKey)))throw new Error("CARDANO_PAYER_SIGNATURE_MISSING");
  const block=latestBlockSchema.parse(await jsonFetch(env,"/blocks/latest"));if(block.slot===null)throw new Error("CARDANO_LATEST_SLOT_UNAVAILABLE");const currentSlot=BigInt(block.slot);if(transaction.ttl<=currentSlot)throw new Error("CARDANO_TTL_EXPIRED");if(transaction.ttl>currentSlot+BigInt(requirement.maxTimeoutSeconds)+CARDANO_TTL_SLOP_SLOTS)throw new Error("CARDANO_TTL_TOO_FAR");if(transaction.validityStart!==undefined&&transaction.validityStart>currentSlot+CARDANO_TTL_SLOP_SLOTS)throw new Error("CARDANO_VALIDITY_NOT_YET_VALID");
  let inputLovelace=0n,inputToken=0n;for(const ref of transaction.inputs){const output=await sourceOutput(env,ref,asset);if(output.address!==env.CARDANO_PAYER_ADDRESS)throw new Error("CARDANO_INPUT_PAYER_MISMATCH");inputLovelace+=output.value.lovelace;inputToken+=output.value.token;}
  let outputLovelace=0n,outputToken=0n,payeeLovelace=0n,payeeToken=0n;for(const output of transaction.outputs){outputLovelace+=output.lovelace;outputToken+=output.token;if(output.addressBytes.equals(payeeDecoded.bytes)){payeeLovelace+=output.lovelace;payeeToken+=output.token;}else if(!output.addressBytes.equals(payerDecoded.bytes))throw new Error("CARDANO_UNAUTHORIZED_OUTPUT");}
  if(asset==="lovelace"){if(inputToken!==0n||outputToken!==0n)throw new Error("CARDANO_MULTI_ASSET_OUTPUT_UNSUPPORTED");if(payeeLovelace!==BigInt(requirement.amount))throw new Error("CARDANO_RECIPIENT_AMOUNT_MISMATCH");}else{if(payeeToken!==BigInt(requirement.amount))throw new Error("CARDANO_RECIPIENT_TOKEN_AMOUNT_MISMATCH");if(inputToken!==outputToken)throw new Error("CARDANO_TOKEN_CONSERVATION_MISMATCH");if(payeeLovelace<=0n)throw new Error("CARDANO_TOKEN_OUTPUT_ADA_REQUIRED");}
  if(inputLovelace!==outputLovelace+transaction.fee)throw new Error("CARDANO_VALUE_CONSERVATION_MISMATCH");const bindingHash=claimBinding(requirement,payload.payload.nonce,env.CARDANO_PAYER_ADDRESS),existing=await readClaim(env,transaction.transactionId);if(existing&&(existing.bindingHash!==bindingHash||existing.network!==network))throw new Error("CARDANO_SETTLEMENT_REPLAY");if(!existing&&!(await nonceIsUnspent(env,env.CARDANO_PAYER_ADDRESS,payload.payload.nonce)))throw new Error("CARDANO_NONCE_NOT_AVAILABLE");if(existing?.state==="REJECTED")throw new Error("CARDANO_SETTLEMENT_DEFINITIVELY_REJECTED");return{transaction,bindingHash,existingClaim:existing};}

async function transactionConfirmations(env:CardanoNativeEnv,transactionId:string){const txRaw=await jsonFetch(env,`/txs/${transactionId}`,undefined,true);if(txRaw===null)return null;const[tx,latest]=await Promise.all([Promise.resolve(transactionEvidenceSchema.parse(txRaw)),jsonFetch(env,"/blocks/latest").then(v=>latestBlockSchema.parse(v))]);if(tx.hash!==transactionId||tx.valid_contract===false||latest.height<tx.block_height)throw new Error("CARDANO_SETTLEMENT_EVIDENCE_MISMATCH");return latest.height-tx.block_height;}
function requiredConfirmations(requirement:Requirement){const value=(requirement.extra.confirmationPolicy as{l1Confirmations?:unknown}|undefined)?.l1Confirmations;return typeof value==="number"&&Number.isInteger(value)&&value>=0&&value<=120?value:1;}
async function waitForConfirmation(env:CardanoNativeEnv,transactionId:string,confirmations:number){const deadline=Date.now()+env.CARDANO_CONFIRMATION_TIMEOUT_MS;do{const observed=await transactionConfirmations(env,transactionId);if(observed!==null&&observed>=confirmations)return true;if(Date.now()>=deadline)break;await new Promise(resolve=>setTimeout(resolve,env.CARDANO_CONFIRMATION_POLL_MS));}while(true);return false;}
async function submitTransaction(env:CardanoNativeEnv,transaction:{bytes:Buffer;transactionId:string}){const body=new ArrayBuffer(transaction.bytes.length);new Uint8Array(body).set(transaction.bytes);const response=await fetch(`${env.CARDANO_BLOCKFROST_URL.replace(/\/$/,"")}/tx/submit`,{method:"POST",headers:{project_id:env.CARDANO_BLOCKFROST_PROJECT_ID,"content-type":"application/cbor",accept:"application/json"},body,signal:AbortSignal.timeout(60000),redirect:"error"});const raw=(await response.text()).trim();if(!response.ok){if(response.status>=400&&response.status<500&&response.status!==408&&response.status!==429)return{kind:"rejected" as const,code:response.status};return{kind:"unknown" as const,code:response.status};}const reported=raw.replace(/^"|"$/g,"").toLowerCase();if(reported&&reported!==transaction.transactionId)return{kind:"unknown" as const,code:502};return{kind:"submitted" as const};}
function publicInvalid(error:unknown){const message=error instanceof Error?error.message:"CARDANO_PAYMENT_INVALID",safe=message.startsWith("CARDANO_")?message.slice(0,120):"CARDANO_PAYMENT_INVALID";return{isValid:false,invalidReason:safe.toLowerCase(),invalidMessage:safe};}

export function createCardanoNativeApp(env:CardanoNativeEnv):{app:Hono;network:CardanoNetwork}{const network=`cardano:${env.CARDANO_NETWORK}` as CardanoNetwork,app=new Hono();
  app.get("/health",c=>c.json({status:"ok",network,x402Version:2,signing:"isolated-remote",assets:["lovelace",...(env.CARDANO_USDCX_ASSET_ID?[env.CARDANO_USDCX_ASSET_ID]:[])]}));
  app.get("/supported",c=>c.json({kinds:[{x402Version:2,scheme:"exact",network,extra:{assetTransferMethods:["default"],settlementLayers:["l1"],areFeesSponsored:false,submissionModes:["server"],resourceBinding:"sha256",assets:["lovelace",...(env.CARDANO_USDCX_ASSET_ID?[env.CARDANO_USDCX_ASSET_ID]:[])]}}]}));
  app.post("/managed-sign",async c=>{if(!bearerSecretMatches(env.MANAGED_SIGNING_API_KEY,env.FACILITATOR_API_KEY,c.req.header("authorization")))return c.json({code:"UNAUTHORIZED"},401);try{const{paymentRequirements}=z.object({paymentRequirements:requirementSchema}).parse(await boundedJson(c.req.raw));if(paymentRequirements.network!==network)return c.json({code:"NETWORK_MISMATCH"},422);allowedAsset(env,paymentRequirements.asset);resourceBinding(paymentRequirements);const signerResponse=await fetch(env.CARDANO_SIGNER_URL,{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${env.CARDANO_SIGNER_API_KEY}`},body:JSON.stringify({network,payerAddress:env.CARDANO_PAYER_ADDRESS,paymentRequirements,submissionMode:"server"}),signal:AbortSignal.timeout(30000),redirect:"error"});if(!signerResponse.ok)return c.json({code:"SIGNER_UNAVAILABLE"},502);const signed=signerResponseSchema.parse(await signerResponse.json()),paymentPayload:ExactPayload={x402Version:2,accepted:paymentRequirements,payload:{transaction:signed.transaction,nonce:signed.nonce,submissionMode:"server"}},verified=await verifyTransaction(env,paymentPayload,paymentRequirements);if(signed.transactionId&&signed.transactionId!==verified.transaction.transactionId)throw new Error("CARDANO_SIGNER_TRANSACTION_ID_MISMATCH");return c.json({paymentPayload,transactionId:verified.transaction.transactionId});}catch(error){console.error(JSON.stringify({event:"cardano_managed_sign_failed",error:error instanceof Error?error.message.slice(0,120):"UNKNOWN"}));return c.json({code:error instanceof Error&&error.message.startsWith("CARDANO_")?error.message:"SIGNING_FAILED"},422);}});
  app.post("/verify",async c=>{if(!bearerSecretMatches(env.SETTLEMENT_API_KEY,env.FACILITATOR_API_KEY,c.req.header("authorization")))return c.json({code:"UNAUTHORIZED"},401);try{const body=requestSchema.parse(await boundedJson(c.req.raw));await verifyTransaction(env,body.paymentPayload,body.paymentRequirements);return c.json({isValid:true,payer:env.CARDANO_PAYER_ADDRESS});}catch(error){return c.json(publicInvalid(error),200);}});
  app.post("/settle",async c=>{if(!bearerSecretMatches(env.SETTLEMENT_API_KEY,env.FACILITATOR_API_KEY,c.req.header("authorization")))return c.json({code:"UNAUTHORIZED"},401);let body:z.infer<typeof requestSchema>,verified:Awaited<ReturnType<typeof verifyTransaction>>;try{body=requestSchema.parse(await boundedJson(c.req.raw));verified=await verifyTransaction(env,body.paymentPayload,body.paymentRequirements);}catch(error){const invalid=publicInvalid(error);return c.json({success:false,errorReason:invalid.invalidReason,errorMessage:invalid.invalidMessage,transaction:"",network},422);}const transactionId=verified.transaction.transactionId;try{let claim=await claimRequest(env,"CLAIM",transactionId,verified.bindingHash);if(claim.state==="CONFIRMED")return c.json({success:true,payer:env.CARDANO_PAYER_ADDRESS,transaction:transactionId,transactionId,network,amount:body.paymentRequirements.amount});if(claim.state==="REJECTED")return c.json({success:false,errorReason:"settlement_definitively_rejected",transaction:transactionId,transactionId,network},422);const confirmations=requiredConfirmations(body.paymentRequirements);if(claim.state==="SUBMISSION_STARTED"){const confirmed=await waitForConfirmation(env,transactionId,confirmations);if(confirmed){await claimRequest(env,"CONFIRM",transactionId,verified.bindingHash);return c.json({success:true,payer:env.CARDANO_PAYER_ADDRESS,transaction:transactionId,transactionId,network,amount:body.paymentRequirements.amount});}return c.json({success:false,errorReason:"payment_pending",errorMessage:"CARDANO_SETTLEMENT_PENDING",transaction:transactionId,transactionId,network},503);}claim=await claimRequest(env,"MARK_SUBMISSION_STARTED",transactionId,verified.bindingHash);if(claim.state!=="SUBMISSION_STARTED")throw new Error("CARDANO_SETTLEMENT_STATE_INVALID");let submission:Awaited<ReturnType<typeof submitTransaction>>;try{submission=await submitTransaction(env,verified.transaction);}catch(error){console.error(JSON.stringify({event:"cardano_submission_unknown",transactionId,error:error instanceof Error?error.name:"UNKNOWN"}));return c.json({success:false,errorReason:"settlement_unknown",errorMessage:"CARDANO_SUBMISSION_UNKNOWN",transaction:transactionId,transactionId,network},503);}if(submission.kind==="rejected"){await claimRequest(env,"REJECT",transactionId,verified.bindingHash);return c.json({success:false,errorReason:"settlement_definitively_rejected",errorMessage:`CARDANO_PROVIDER_${submission.code}`,transaction:transactionId,transactionId,network},422);}if(submission.kind==="unknown")return c.json({success:false,errorReason:"settlement_unknown",errorMessage:`CARDANO_PROVIDER_${submission.code}`,transaction:transactionId,transactionId,network},503);const confirmed=await waitForConfirmation(env,transactionId,confirmations);if(!confirmed)return c.json({success:false,errorReason:"payment_pending",errorMessage:"CARDANO_SETTLEMENT_PENDING",transaction:transactionId,transactionId,network},503);await claimRequest(env,"CONFIRM",transactionId,verified.bindingHash);return c.json({success:true,payer:env.CARDANO_PAYER_ADDRESS,transaction:transactionId,transactionId,network,amount:body.paymentRequirements.amount});}catch(error){console.error(JSON.stringify({event:"cardano_settlement_failed",transactionId,error:error instanceof Error?error.message.slice(0,120):"UNKNOWN"}));const reason=error instanceof Error&&error.message==="CARDANO_SETTLEMENT_REPLAY"?"duplicate_settlement":"settlement_unknown";return c.json({success:false,errorReason:reason,errorMessage:reason==="duplicate_settlement"?"CARDANO_SETTLEMENT_REPLAY":"CARDANO_SETTLEMENT_CONTROL_PLANE_UNAVAILABLE",transaction:transactionId,transactionId,network},reason==="duplicate_settlement"?422:503);}});
  return{app,network};}
