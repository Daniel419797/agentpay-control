import { db } from "@/lib/db"; import { handleApiError, ok, problem } from "@/lib/api";
export async function GET(_:Request,{params}:{params:Promise<{transactionId:string}>}){try{const {transactionId}=await params;const i=await db.paymentIntent.findUnique({where:{id:transactionId},include:{agent:true,quote:{include:{asset:true}},decisions:true,approval:true,reservation:true,attempts:{include:{settlement:true}}}});if(!i)return problem(404,"TRANSACTION_NOT_FOUND","Transaction not found.");return ok({...i,quote:i.quote?{...i.quote,amountAtomic:i.quote.amountAtomic.toString()}:null,reservation:i.reservation?{...i.reservation,amountAtomic:i.reservation.amountAtomic.toString()}:null,decisions:i.decisions.map(d=>({...d,spendBeforeAtomic:d.spendBeforeAtomic.toString(),reservedBeforeAtomic:d.reservedBeforeAtomic.toString(),projectedAtomic:d.projectedAtomic.toString()})),attempts:i.attempts.map(a=>({...a,settlement:a.settlement?{...a.settlement,amountAtomic:a.settlement.amountAtomic.toString()}:null}))});}catch(e){return handleApiError(e)}}
