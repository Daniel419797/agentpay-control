import { db } from "@/lib/db"; import { handleApiError, ok, problem } from "@/lib/api";
export async function GET(_:Request,{params}:{params:Promise<{intentId:string}>}){try{const {intentId}=await params;const row=await db.paymentIntent.findUnique({where:{id:intentId},include:{agent:true,quote:{include:{asset:true}},decisions:true,approval:true,attempts:{include:{settlement:true}}}});if(!row)return problem(404,"PAYMENT_INTENT_NOT_FOUND","Payment intent not found.");return ok({...row,quote:row.quote?{...row.quote,amountAtomic:row.quote.amountAtomic.toString()}:null,decisions:row.decisions.map(d=>({...d,spendBeforeAtomic:d.spendBeforeAtomic.toString(),reservedBeforeAtomic:d.reservedBeforeAtomic.toString(),projectedAtomic:d.projectedAtomic.toString()})),attempts:row.attempts.map(a=>({...a,settlement:a.settlement?{...a.settlement,amountAtomic:a.settlement.amountAtomic.toString()}:null}))});}catch(e){return handleApiError(e)}}
