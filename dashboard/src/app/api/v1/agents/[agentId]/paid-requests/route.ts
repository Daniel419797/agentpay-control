import { z } from "zod"; import { createPaidRequest } from "@/domain/payment-service"; import { authorizeAgentRequest, boundedJson, handleApiError, ok, problem, rateLimitProblem } from "@/lib/api"; import { enforceRateLimit } from "@/lib/rate-limit";
const schema=z.object({resourceUrl:z.string().url(),purpose:z.string().max(300).optional(),maxAmountAtomic:z.string().regex(/^\d+$/).optional()});
export async function POST(request:Request,{params}:{params:Promise<{agentId:string}>}){const key=request.headers.get("idempotency-key");if(!key)return problem(400,"IDEMPOTENCY_KEY_REQUIRED","Idempotency-Key is required.");try{const {agentId}=await params;if(!await authorizeAgentRequest(request,agentId,"payments:create"))return problem(401,"UNAUTHORIZED","A valid agent credential with payments:create is required.");const rate=await enforceRateLimit(request,{scope:"agent-paid-request",subject:agentId,limit:60,windowMs:60_000});if(!rate.allowed)return rateLimitProblem(rate.retryAfterSeconds);const result=await createPaidRequest(agentId,key,schema.parse(await boundedJson(request)));return ok(result,{status:result.status==="APPROVAL_PENDING"?202:200});}catch(e){if(e instanceof Error){const codes:Record<string,number>={IDEMPOTENCY_CONFLICT:409,POLICY_NOT_PUBLISHED:409,RESOURCE_PRICE_NOT_FOUND:404,MAX_AMOUNT_EXCEEDED:422,RESOURCE_URL_UNSAFE:422,LIVE_FACILITATOR_REQUIRED:503,MANAGED_SIGNER_REQUIRED:409,PAYMENT_ACCOUNT_UNAVAILABLE:503,ORGANIZATION_KILL_SWITCH_ENABLED:409,AGENT_NOT_ACTIVE:409,PAYMENT_QUOTE_EXPIRED:409,SPEND_RESERVATION_INVALID:409,POLICY_CHANGED:409,POLICY_NOT_ACTIVE:409,POLICY_EXPIRED:409,OUTSIDE_POLICY_SCHEDULE:409};if(codes[e.message])return problem(codes[e.message],e.message,e.message.replaceAll("_"," "));}return handleApiError(e)}}
