import { db } from "@/lib/db"; import { handleApiError, ok, problem } from "@/lib/api"; import { workspaceFromRequest, workspaceHasRole } from "@/lib/workspace";
export async function GET(request:Request,{params}:{params:Promise<{agentId:string}>}){try{const workspace=await workspaceFromRequest(request);if(!workspace)return problem(401,"AUTH_REQUIRED","Sign in before viewing policies.");if(!workspaceHasRole(workspace,["OWNER","OPERATOR","VIEWER"]))return problem(403,"ROLE_REQUIRED","Policy access is required.");const {agentId}=await params;const agent=await db.agent.findFirst({where:{id:agentId,organizationId:workspace.organization.id},include:{effectivePolicy:{include:{asset:true}},policies:{include:{versions:{where:{status:"DRAFT"},orderBy:{version:"desc"},take:1}}}}});if(!agent)return problem(404,"AGENT_NOT_FOUND","Agent not found.");const policy=agent.effectivePolicy;return ok({published:policy?{...policy,perTransactionLimitAtomic:policy.perTransactionLimitAtomic.toString(),dailyLimitAtomic:policy.dailyLimitAtomic.toString()}:null,draft:agent.policies[0]?.versions[0]??null});}catch(e){return handleApiError(e)}}
