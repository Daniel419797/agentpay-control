import { createHash, randomBytes } from "node:crypto"; import { z } from "zod"; import { db } from "@/lib/db"; import { handleApiError, ok } from "@/lib/api";
const schema=z.object({label:z.string().min(2).max(80),scopes:z.array(z.enum(["payments:create","payments:read","resources:read"])).min(1),expiresAt:z.string().datetime().optional()});
export async function GET(_:Request,{params}:{params:Promise<{agentId:string}>}){try{const {agentId}=await params;return ok(await db.agentCredential.findMany({where:{agentId},select:{id:true,label:true,prefix:true,scopes:true,status:true,expiresAt:true,lastUsedAt:true,createdAt:true}}));}catch(e){return handleApiError(e)}}
export async function POST(request:Request,{params}:{params:Promise<{agentId:string}>}){try{const {agentId}=await params;const input=schema.parse(await request.json());const secret=`ap_test_${randomBytes(24).toString("base64url")}`;const prefix=secret.slice(0,14);const credential=await db.agentCredential.create({data:{agentId,label:input.label,prefix,secretHash:createHash("sha256").update(secret).digest("hex"),scopes:input.scopes,expiresAt:input.expiresAt?new Date(input.expiresAt):undefined}});return ok({id:credential.id,label:credential.label,prefix,scopes:credential.scopes,secret,warning:"This secret is shown once."},{status:201});}catch(e){return handleApiError(e)}}
