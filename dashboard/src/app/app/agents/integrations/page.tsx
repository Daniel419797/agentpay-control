import { FormPage } from "@/components/workspace-page";

export default function IntegrationsDirectoryPage() {
  return <FormPage title="AI integrations" description="Choose an AgentPay payment agent to configure an external AI client." />;
}
