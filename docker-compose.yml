services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: agentpay
      POSTGRES_PASSWORD: agentpay
      POSTGRES_DB: agentpay
    ports:
      - "54329:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agentpay -d agentpay"]
      interval: 3s
      timeout: 3s
      retries: 20
    volumes:
      - agentpay-postgres:/var/lib/postgresql/data

volumes:
  agentpay-postgres:
